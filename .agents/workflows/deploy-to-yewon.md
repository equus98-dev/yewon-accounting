---
description: Cloudflare Pages (yewon.pages.dev) 배포 절차
---
// turbo-all
모든 배포는 `f:\2. 업무폴더\4_예원예술대학교\Accounting system` 디렉토리의 `deploy.bat`을 실행하여 `yewon.pages.dev`에 반영합니다.

1. 배포 전 `wrangler.toml`의 이름이 `yewon`으로 되어 있는지 확인합니다.
2. `deploy.bat` 파일을 실행합니다.
   ```powershell
   .\deploy.bat
   ```
3. `yewon.pages.dev` 주소로 접속하여 정상 반영 여부를 확인합니다.
